NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Para acceder desde el teléfono en la misma red WiFi,
# usa la IP de tu PC (ej. la que muestra Next.js en "Network"):
# NEXT_PUBLIC_API_URL=http://192.168.1.100:4000/api
#
# Si no se define, la app detecta automáticamente la IP del navegador.
